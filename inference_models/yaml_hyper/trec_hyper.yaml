epochs: 10

loss: 'binary_crossentropy'
opt_gen_lr: 0.001
criterion: 'CrossEntropyLoss'
metrics:
  - 'accuracy'

binary: False

early_stopping_patience: 3

embedding_dim: 100

model_saved: 'inference_models/fast_model.pt'

inner_epochs: 5